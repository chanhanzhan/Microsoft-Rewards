# WebUI 使用指南

## 启动 WebUI

使用以下命令启动 WebUI 管理界面：

```bash
python main.py --mode webui
```

默认访问地址：`http://localhost:5000`

## 功能详解

### 1. 仪表盘

查看系统运行状态：
- **自动化状态**: 显示调度器是否运行
- **账户数量**: 已配置的账户总数
- **下次运行时间**: 自动化任务的下次执行时间
- **日志条数**: 已记录的执行日志数量

控制按钮：
- **启动自动化**: 启动定时任务调度器
- **停止自动化**: 停止定时任务调度器
- **刷新状态**: 手动刷新状态信息

### 2. 账户管理

#### 查看账户列表
- 显示所有已配置的账户
- 图标说明：
  - 🔐 = 已保存密码
  - 🍪 = Cookie 已存在
- 实时显示账户积分信息

#### 添加账户
1. 输入 Microsoft 账户用户名
2. 输入密码（可选，建议填写以支持自动刷新 Cookie）
3. 点击"添加账户"

#### 管理账户
每个账户提供以下操作：
- **查询积分**: 实时查询 Microsoft Rewards 积分
  - 显示可用积分
  - 显示总计积分
  - 显示今日进度
- **刷新Cookie**: 手动触发 Cookie 刷新（会打开浏览器登录）
- **编辑**: 修改账户用户名和密码
- **删除**: 删除账户及其 Cookie 文件

### 3. 配置管理

#### 自动化配置
- **启用自动化**: 开启/关闭定时任务
- **运行模式**:
  - `random_24h`: 24小时内随机时间运行（默认）
  - `fixed`: 固定时间运行
- **启动时立即运行**: 程序启动时是否立即执行一次任务

#### 执行配置
- **最大搜索次数**: 每次运行的搜索次数（1-100）
- **基础延迟**: 搜索之间的随机延迟时间范围（秒）
  - 最小值：延迟的最小时间
  - 最大值：延迟的最大时间
- **重试延迟**: 失败后重试前的等待时间（秒）
- **最大重试次数**: 连续失败的最大重试次数
- **模拟打字**: 是否模拟人工逐字输入
- **无头模式**: 是否使用无头浏览器（不显示界面）

#### 设备配置
- **启用PC设备**: 是否执行PC端搜索任务
- **PC User-Agent**: PC浏览器的User-Agent字符串
- **启用移动设备**: 是否执行移动端搜索任务
- **Mobile User-Agent**: 移动浏览器的User-Agent字符串

#### WebUI配置
- **监听地址**: WebUI监听的IP地址（默认 0.0.0.0 允许外部访问）
- **监听端口**: WebUI监听的端口号（默认 5000）

修改配置后，点击"保存配置"按钮使配置生效。

### 4. 手动执行

在此页面可以手动触发任务执行：

1. **选择账户**: 
   - 选择"所有账户"将执行所有已配置账户
   - 选择特定账户仅执行该账户的任务

2. **选择设备**:
   - `全部设备`: 执行PC和移动设备任务
   - `PC`: 仅执行PC设备任务
   - `移动设备`: 仅执行移动设备任务

3. 点击"开始执行"启动任务

任务将在后台运行，可在"执行日志"页面查看进度。

### 5. 执行日志

查看历史执行记录：
- 显示最近100条执行日志
- 包含时间戳、账户、设备和执行结果
- 点击"刷新日志"获取最新日志

## 配置文件

WebUI 的所有配置会自动保存到以下文件：

- `config.json`: 系统配置
- `account.json`: 账户配置
- `cookie_<username>.txt`: 各账户的Cookie文件

这些文件会在首次运行时自动创建。

## 高级功能

### 积分查询原理
- WebUI 通过加载账户 Cookie 访问 Microsoft Rewards 页面
- 自动解析页面数据获取积分信息
- 支持查询可用积分、总计积分和每日进度

### Cookie 刷新流程
1. 点击"刷新Cookie"按钮
2. 系统会打开Chrome浏览器
3. 自动填充用户名和密码（如果已保存）
4. 等待浏览器完成登录流程
5. 系统自动提取并保存新的Cookie

### 自动化调度机制
- 启用自动化后，系统会根据配置计算下次运行时间
- `random_24h`模式：在接下来24小时内随机选择一个时间
- 每天只运行一次，避免频繁操作被检测
- 支持跨时区配置

## 注意事项

1. **首次使用**：
   - 必须先添加账户并保存密码
   - 建议先手动刷新一次Cookie确保登录成功

2. **积分查询**：
   - 需要有效的Cookie才能查询积分
   - 查询失败时请尝试刷新Cookie

3. **安全建议**：
   - 如果外网访问，建议修改监听地址为 `127.0.0.1`
   - 定期更新密码和Cookie
   - 不要分享Cookie文件

4. **性能优化**：
   - 启用无头模式可以减少资源占用
   - 调整延迟时间可以控制执行速度

## 常见问题

**Q: WebUI 无法访问？**
A: 检查防火墙设置，确保端口5000未被占用。

**Q: 积分查询失败？**
A: Cookie可能已过期，点击"刷新Cookie"重新登录。

**Q: 自动化不运行？**
A: 检查配置中"启用自动化"是否勾选，查看日志了解详情。

**Q: 如何修改端口？**
A: 在配置管理页面修改WebUI端口，保存后需要重启程序。

**Q: 支持多账户吗？**
A: 完全支持，可以添加任意数量的账户。
